# ===============================================
# TERRAFORM PROJECT STRUCTURE
# ===============================================
# environments/prod/ - Production environment
# environments/dev/  - Development environment  
# modules/          - Reusable Terraform modules
# ===============================================

# Terraform state files (environments manage these via S3 backend)
environments/*/*.tfstate
environments/*/*.terraform

environments/*/*.tfstate.*
environments/*/*.tfstate.backup

modules/lambda-authorizer/dist
modules/lambda-authorizer/lambda-authorizer.zip


# Terraform directories in environments
environments/prod/.terraform/
environments/dev/.terraform/
environments/prod/.terraform.lock.hcl
environments/dev/.terraform.lock.hcl
# Alternative patterns for .terraform directories
*/.terraform/
*/.terraform.lock.hcl

# Any local Terraform files (if created outside environments)
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Terraform variables files (podem conter secrets)
environments/*/*.tfvars.json
# Any local tfvars files
*.tfvars.json

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Lambda build artifacts
lambda-authorizer.zip
*.zip

# Node.js dependencies (including in modules)
node_modules/
modules/lambda-authorizer/node_modules/
package-lock.json

# TypeScript build output (including in modules)
dist/
build/
modules/lambda-authorizer/dist/
modules/lambda-authorizer/build/
modules/lambda-authorizer/src.zip

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Log files
*.log

# Temporary files
*.tmp
*.temp

# Environment files
.env
.env.local
.env.*.local

# Debug files
debug-*.js